export enum TaskStatus {
  TODO = "todo",
  IN_PROGRESS = "in_progress",
  DONE = "done",
}

export enum TaskStatusLabel {
  TODO = "To Do",
  IN_PROGRESS = "In Progress",
  DONE = "Done",
}

export enum TaskPriority {
  HIGH = 1,
  MEDIUM = 2,
  LOW = 3,
}

export enum TaskPriorityLabel {
  HIGH = "High",
  MEDIUM = "Medium",
  LOW = "Low",
}

export enum TaskPriorityColor {
  HIGH = "bg-red-500",
  MEDIUM = "bg-yellow-500",
  LOW = "bg-green-500",
}
